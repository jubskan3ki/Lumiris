/**
 * Root Prettier config — re-exports the shared rules from @lumiris/config.
 * Editor integrations resolve this file automatically.
 */
import config from '@lumiris/config/prettier';

export default config;
