/**
 * Root Stylelint config — re-exports the shared rules from @lumiris/config.
 * Stylelint resolves this file automatically when invoked at the repo root.
 */
import config from '@lumiris/config/stylelint';

export default config;
