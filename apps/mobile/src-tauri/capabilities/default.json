{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Default capability for the Lumiris Mobile shell — only exposes the compute_score command and core window controls.",
    "windows": ["main"],
    "permissions": ["core:default"]
}
